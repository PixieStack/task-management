# 🚀 QUICK COMMANDS FOR fix-dashboard-2 BRANCH

## Copy-Paste These Commands on Your Windows Machine:

### 1️⃣ CREATE .env FILE

Navigate to `backend/` folder and create `.env` file with this content:

```env
SECRET_KEY=iVk5KKN70TbRcpJGoS3z41RfyJBV_-y9Z7pVyVcsxxg
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440
DATABASE_URL=sqlite:///./taskmanager.db
```

### 2️⃣ RUN BACKEND (Terminal 1)

```powershell
cd C:\Users\thwal\Documents\projects\task-management\backend
.\venv\Scripts\Activate.ps1
pip install fastapi uvicorn sqlalchemy python-jose passlib bcrypt python-dotenv pydantic email-validator python-multipart
python -m uvicorn server:app --reload --host 0.0.0.0 --port 8001
```

### 3️⃣ RUN FRONTEND (Terminal 2)

```powershell
cd C:\Users\thwal\Documents\projects\task-management\frontend
yarn start
```

### 4️⃣ TEST

- Backend API: http://localhost:8001/docs
- Frontend App: http://localhost:3000

---

## ✅ WHAT'S FIXED

1. ❌ Removed `asyncpg` (PostgreSQL - not needed)
2. ✅ Using SQLite (built-in, no installation)
3. ✅ Windows-compatible packages only
4. ✅ 10 packages instead of 33

---

## 🎯 AFTER IT WORKS

Let me know when backend starts successfully, then I'll:
1. Update dashboard component to match your designs
2. Connect all features to backend (challenges, diet, AI, etc.)
3. Fix profile & navbar
4. Make everything save to database

---

Full guide: `/app/WINDOWS_SETUP_GUIDE.md`
