The OAuth smoke test mocks the provider-token verification boundary so CI can deterministically exercise TaskManager account creation/linking without storing live Google or Apple credentials in GitHub Actions. The production verifier itself validates provider signing keys, audience, issuer and subject before returning claims to the auth router. Live provider-console configuration is still required for real browser sign-in.
